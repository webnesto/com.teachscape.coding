'use strict';

describe('com.teachscape.coding.confirmation module', function() {

  beforeEach(module('com.teachscape.coding.confirmation'));

  describe('reservation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var reservationCtrl = $controller('ReservationCtrl');
      expect(reservationCtrl).toBeDefined();
    }));

  });
});