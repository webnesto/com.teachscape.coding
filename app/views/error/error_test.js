'use strict';

describe('com.teachscape.coding.error module', function() {

  beforeEach(module('com.teachscape.coding.error'));

  describe('reservation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var reservationCtrl = $controller('ErrorCtrl');
      expect(reservationCtrl).toBeDefined();
    }));

  });
});