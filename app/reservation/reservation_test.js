'use strict';

describe('com.teachscape.coding.reservation module', function() {

  beforeEach(module('com.teachscape.coding.reservation'));

  describe('reservation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var reservationCtrl = $controller('ReservationCtrl');
      expect(reservationCtrl).toBeDefined();
    }));

  });
});