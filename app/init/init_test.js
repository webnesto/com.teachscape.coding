'use strict';

describe('com.teachscape.coding.init module', function() {

  beforeEach(module('com.teachscape.coding.init'));

  describe('init controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var initCtrl = $controller('InitCtrl');
      expect(initCtrl).toBeDefined();
    }));

  });
});