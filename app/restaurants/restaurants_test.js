'use strict';

describe('com.teachscape.coding.restaurants module', function() {

  beforeEach(module('com.teachscape.coding.restaurants'));

  describe('restaurants controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var restaurantsCtrl = $controller('RestaurantsCtrl');
      expect(restaurantsCtrl).toBeDefined();
    }));

  });
});